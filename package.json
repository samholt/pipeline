{
  "name": "distill-pipeline",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "yarn install && bower install && git clone https://github.com/distillpub/distill-template.git",
    "clone": "node bin/clone",
    "build": "(cd distill-template/; git pull; yarn build) && yarn clone && node bin/build",
    "serve": "static docs/ -a localhost"
  },
  "dependencies": {
    "d3": "^4.4.1",
    "d3-collection": "^1.0.2",
    "d3-dsv": "^1.0.3",
    "d3-time-format": "^2.0.3",
    "distill-template": "^0.0.21",
    "jsdom": "^9.8.3",
    "mustache": "^2.3.0"
  },
  "devDependencies": {}
}
